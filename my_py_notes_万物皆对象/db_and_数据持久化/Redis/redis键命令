## redis键命令
#### 1.keys *: 查看所有键
```html
keys *
```
#### 2. 使用通配符 *：查看名称中包含a的键
```
keys '*a*'
```
#### 3. exists 判断键是否存在：如果存在返回1，不存在返回0
```
exists key1 keys2 ...
```
#### 4. type 查看键对应的value的类型
```
type key
```
#### 5. del 删除键及对应的值
```
del key1 key2 ...
```
#### 6. expire 设置过期时间
* 以秒为单位，如果没有指定过期时间则一直存在，直到使用DEL移除
```
expire key 60
```
#### 7. ttl 查看有效时间，以秒为单位
```
ttl key
```
