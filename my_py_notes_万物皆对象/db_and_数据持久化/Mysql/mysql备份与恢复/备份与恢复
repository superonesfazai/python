## 备份
* 运行mysqldump命令
```shell
python@ubuntu:~$ mysqldump –uroot –p 数据库名 > ~/Desktop/py.sql;
按提示输入mysql的密码
```

## 恢复
* 连接mysql, 创建数据库
* 退出连接, 执行下面命令
```shell
python@ubuntu:~$ mysql -u root -p 数据库名 < ~/Desktop/py.sql
根据提示输入mysql密码
```